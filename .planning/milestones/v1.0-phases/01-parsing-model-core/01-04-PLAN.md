---
phase: 01-parsing-model-core
plan: 04
type: execute
wave: 4
gap_closure: true
gap_ids: [G-01-4, G-01-7, G-01-8]
depends_on: [01-01, 01-02, 01-03]
files_modified:
  - src/csv.ts
  - src/index.ts
  - test/csv.test.ts
  - test/tracer.test.ts
autonomous: true
requirements: [PARSE-07]
estimate:
  tokens: 24000
  raw_tokens: 12000
  tasks: 2
  confidence: high

must_haves:
  truths:
    - "Every CSV row occupies exactly ONE physical line: embedded CR/LF in message text is written as the literal two-character sequence \\n (backslash-n), and a literal backslash is written as \\\\ — no raw line breaks inside fields."
    - "readCsv reverses the escaping exactly, so merge round-trips preserve real newlines in Message.text (write → read → write is byte-stable)."
    - "The CLI accepts `--zip <path>` as an alias for the positional ZIP argument; passing both errors, neither errors with usage."
    - "`wa-backup --help` shows copy-paste examples including the npm form `npm run dev -- <zip> --verbose` so flags are not eaten by npm."
  artifacts:
    - "src/csv.ts — csvField escapes \\\\ and \\n / \\r; readCsv unescapes after RFC-4180 parse"
    - "src/index.ts — optional positional + --zip option alias + help examples"
    - "test/csv.test.ts — escaping round-trip tests"
    - "test/tracer.test.ts — continuation row asserted via escaped form"
---

<objective>
Close the three UAT gaps from phase 1 testing: multi-line CSV rows that break line-based tooling (G-01-4) and CLI flags unreachable through npm invocation forms (G-01-7, G-01-8).
</objective>

<context>
@.planning/phases/01-parsing-model-core/01-UAT.md
@src/csv.ts
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escape CR/LF in CSV fields (G-01-4)</name>
  <files>src/csv.ts, test/csv.test.ts, test/tracer.test.ts</files>
  <action>
    - In `csvField`: FIRST replace `\\` → `\\\\`, then `\r\n|\n|\r` → literal `\\n`; THEN apply RFC-4180 quoting when the result contains `"` or `,` (newlines can no longer occur).
    - In `readCsv`: after parsing each field, reverse exactly: `\\n`/`\\r` → real chars, then `\\\\` → `\\`. Round-trip property: `readCsv(writeCsv(rows)) == rows`.
    - Update `test/tracer.test.ts` continuation assertion: the FILE now contains `Rede: Conexão WK - Staff\\nSenha: TIMEWK2026` on one physical line; parsed records still expose the real newline via readCsv semantics.
    - Add csv tests: single physical line per row (no `\\n` byte between row start and row end except terminators); backslash round-trip (`a\\nb` text survives); merge re-write stays stable.
  </action>
  <acceptance_criteria>
    - `grep -c $'\n' messages.csv` equals row count + header (no embedded breaks) for a fresh parse of Plataforma WK.
    - All existing tests updated and green; new round-trip tests green.
  </acceptance_criteria>
  <verify><automated>npm test</automated></verify>
  <done>CSV is one physical line per row with lossless escape round-trip.</done>
</task>

<task type="auto">
  <name>Task 2: CLI --zip alias + flag-safe help (G-01-7, G-01-8)</name>
  <files>src/index.ts</files>
  <action>
    - Make the positional argument optional (`[zip]`) and add `.option('--zip <path>', 'path to the export ZIP (alternative to positional)')`. Resolution: exactly one of positional/--zip required — if both or neither, print usage error and exit 1.
    - Enrich `.help` text (addHelpText / description examples) with three copy-paste forms: direct tsx, built bin, and `npm run dev -- "<zip>" --verbose` (emphasize the `--` separator so npm passes flags through).
  </action>
  <acceptance_criteria>
    - `npx tsx src/index.ts --zip <real.zip> --out /tmp/x --verbose` prints the detection report.
    - Both-neither cases exit non-zero with clear message.
    - `--help` displays the npm example.
  </acceptance_criteria>
  <verify><automated>npm test && npx tsx src/index.ts --help</automated></verify>
  <done>Flags reach the parser through documented invocation forms.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition |
|-----------|----------|-----------|-------------|
| T-01-02b | Injection | escape/unescape symmetry | mitigate — unescape runs only on our own written files during merge; malformed external edits pass through unchanged |
</threat_model>

<verification>
- `npm test` all suites green.
- Manual: `npx tsx src/index.ts "data/WhatsApp Chat - Plataforma WK.zip" --out /tmp/uat-fix --verbose` prints report; messages.csv has one physical line per row.
</verification>

<success_criteria>
All three UAT gaps closed: CSV rows are single physical lines with lossless escaping, and CLI flags work through both direct and npm invocation forms.
</success_criteria>

<output>
Create `.planning/phases/01-parsing-model-core/01-04-SUMMARY.md` when done.
</output>
