---
phase: 01-parsing-model-core
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/parse/timestamp.ts
  - src/parse/message.ts
autonomous: true
requirements:
  - PARSE-03
  - PARSE-07
estimate:
  tokens: 64000
  raw_tokens: 32000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Day/month order for ambiguous dates (both day≤12 and month≤12) is resolved by a best-effort MAJORITY VOTE over the first ~50 timestamped lines; `--day-first`/`--month-first` override and short-circuit detection (D-01)."
    - "12h vs 24h is detected PER-FILE: if ANY `AM`/`PM` token appears, ALL times are 12h (PM && h!=12 → h+=12; AM && h==12 → h=0); otherwise 24h (D-02)."
    - "Both iOS `[DD/MM/YYYY, HH:MM:SS]` and Android `DD/MM/YYYY, HH:MM:SS` styles, `.`/`-`/`/` separators, and optional seconds are accepted per-line by `TS_RE` (D-03)."
    - "A line matching `TS_RE` but yielding an invalid date (e.g. `31/02`) is treated as a CONTINUATION of the previous message, not a new one (D-04)."
    - "A date whose year < 2009 or > currentYear+1 is a sanity-window violation → treated as continuation + reported in `--verbose` (D-08)."
    - "2-digit years use the sliding window `yy <= (currentYear-2000+1) ? 2000+yy : 1900+yy` (D-05)."
    - "`--verbose` reports detected format, day/month decision, 12h/24h decision, and any applied overrides/continuation warnings (D-07)."
    - "`<Media omitted>` / `* omitted` / `image omitted` / `video omitted` / `sticker omitted` / `document omitted` → `type='omitted'` with the marker preserved as visible `text` (MEDIA-04 placeholder, D-15)."
    - "`Mensagem apagada` / `Message deleted` / `This message was deleted` → `type='deleted'` (D-15)."
    - "The `author` column preserves the RAW sender string including `~ ` prefixes and parenthesized/phone bidi wrappers; values are never normalized in Phase 1 (D-09, PARSE-05)."
  artifacts:
    - "src/parse/timestamp.ts — `detectFormat(sampleLines, {dayFirst,monthFirst})` returning `{dayFirst:boolean, is12h:boolean, year2window, detectedExample}`, consumed by `parseTimestamp`"
    - "src/parse/timestamp.ts — `parseTimestamp(stripped, detection)` extended with 12h conversion, ambiguous D/M resolution, sanity-window guard → returns null on invalid/out-of-range (continuation signal)"
    - "src/parse/message.ts — `classifyType(body, media)` handling `omitted`/`deleted` markers and `system` fallback"
    - "src/model.ts — `runParser` gains a two-pass detection: sample first N lines for format, then parse"
  key_links:
    - "Detection must run once over a SAMPLE of lines, then be applied to every line (per-file decision, D-01/D-02)"
    - "Invalid/out-of-range dates must fall through to the continuation branch of the message state machine (D-04/D-08)"
    - "`classifyType` ordering: attached→omitted-marker→deleted-marker→system→text (research §3.3)"
---

<objective>
Hardens the tracer's parser with full locale-tolerant timestamp detection (PARSE-03) and the omitted/deleted/system type classification that the tracer stubbed as `text`. This is the expansion that makes the parser correct across locales and message kinds.

Purpose: Satisfy PARSE-03 completely and complete PARSE-07's `type` granularity (omitted/deleted), plus transparent `--verbose` reporting (D-07).
Output: `detectFormat` + 12h/D-M/2-digit-year/sanity-window logic in timestamp.ts; `classifyType` omitted/deleted in message.ts.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-model-core/01-CONTEXT.md
@.planning/phases/01-parsing-model-core/01-RESEARCH.md
@data/WhatsApp Chat - Notas pessoais/_chat.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locale-tolerant timestamp detection (PARSE-03)</name>
  <files>src/parse/timestamp.ts, src/model.ts</files>
  <read_first>
    - src/parse/timestamp.ts (current tracer implementation from plan 01)
    - .planning/phases/01-parsing-model-core/01-RESEARCH.md (§2.3 day/month vote, §2.4 12h/24h, §2.5 sanity + 2-digit year)
    - .planning/phases/01-parsing-model-core/01-CONTEXT.md (D-01..D-05, D-07, D-08)
  </read_first>
  <action>
    Extend `src/parse/timestamp.ts` (do NOT break the tracer's happy path):
    - Add `detectFormat(lines: string[], opts: {dayFirst?: boolean; monthFirst?: boolean}): { dayFirst: boolean; is12h: boolean; example: string }`.
      - If `opts.dayFirst` → `dayFirst=true`; if `opts.monthFirst` → `dayFirst=false` (override short-circuits, D-01).
      - Else scan the first up-to-50 timestamped lines (after `stripInvisible`, `TS_RE` match). For each ambiguous line (both `day<=12 && month<=12`), tentatively count how many would be VALID dates under day-first vs month-first (`day<=31 && month<=12`); choose the ordering with more valid dates; tie → default DAY-FIRST (pt-BR, assumption A2).
      - `is12h`: scan ALL sampled lines for any `AM`/`PM`/`am`/`pm` token → `is12h=true` (D-02).
    - Extend `parseTimestamp(stripped, detection)`:
      - Apply `detection.dayFirst` to assign day/month from the two captured groups.
      - If `detection.is12h` and an am/pm token present: `if (pm && h!==12) h+=12; if (am && h===12) h=0;` (D-02).
      - 2-digit year via sliding window `yy <= (currentYear-2000+1) ? 2000+yy : 1900+yy` (D-05).
      - Build LOCAL `Date`; if invalid (e.g. `31/02`) OR year<2009 OR year>currentYear+1 → return `null` (signals continuation per D-04/D-08).
      - Else `iso = format(d, "yyyy-MM-dd'T'HH:mm:ss")` (D-06/D-18).
    - In `src/model.ts` `runParser`: before the main parse, read a SAMPLE of lines (first ~200 via a bounded readline pass or by peeking the extract interface) and call `detectFormat` to build `detection`; pass `detection` into `parseMessages`/`parseTimestamp`. Keep `opts.dayFirst`/`monthFirst` wired from CLI (already present).
    - Add a `verboseReport(detection, warnings)` helper (picocolors) printed when `opts.verbose` is set, reporting: detected day/month order, 12h/24h, example timestamp, and any override applied (D-07).
  </action>
  <acceptance_criteria>
    - Unit assertions in a new `test/timestamp.test.ts` (node:test):
      - `[01/02/2026, 03:04:05 PM] Name: x` with no override → `detectFormat` sets `is12h=true`; `parseTimestamp` yields `15:04:05` (PM conversion, D-02).
      - Ambiguous file where majority of dates are `DD/MM` → `dayFirst=true`; a file where majority are `MM/DD` → `dayFirst=false`; `--day-first` forces true regardless.
      - `[31/02/2026, 10:00:00] Name: x` → `parseTimestamp` returns `null` (invalid date → continuation, D-04).
      - `[01/01/2099, 10:00:00] Name: x` → `null` (sanity-window violation, D-08).
      - 2-digit year `[15/08/26, 10:00:00]` → year 2026; `[15/08/99, 10:00:00]` → year 1999 (D-05).
      - Android style `15/08/2026, 10:00:00 Name: x` (no brackets) still parses (D-03).
    - `npm test` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full locale-tolerant detection (day/month vote, 12h/24h, separators, 2-digit year, sanity window, invalid→continuation) is implemented and unit-tested; `--verbose` reports the decisions.</done>
  <reversibility rating="costly">D-03/D-05 detection shapes the persisted `timestamp_iso`; changing later would require re-parsing. Already user-locked via D-03/D-05 in CONTEXT — no re-ask.</reversibility>
</task>

<task type="auto">
  <name>Task 2: omitted/deleted/system type classification + verbose reporting (PARSE-07)</name>
  <files>src/parse/message.ts, src/model.ts</files>
  <read_first>
    - src/parse/message.ts (current tracer state machine from plan 01)
    - .planning/phases/01-parsing-model-core/01-RESEARCH.md (§3.3 media/type rules, §7 risks table for omitted/deleted)
    - data/WhatsApp Chat - Notas pessoais/_chat.txt (lines 6,14,15,18,36,50,57,69,74,120-121,196 — omitted/deleted/document-omitted real cases)
    - .planning/phases/01-parsing-model-core/01-CONTEXT.md (D-09 raw author, D-11 system, D-15 8 types)
  </read_first>
  <action>
    In `src/parse/message.ts`, replace the tracer's bare `type='text'`/`type='system'` assignment with a `classifyType(body, media): MessageType` applied per message (research §3.3 ordering):
    1. If `media` is set from `<attached>` → keep the filename-derived type (sticker/photo/video/document) already assigned (no change to tracer behavior).
    2. Else if `body` matches `/<?(?:media|image|video|sticker|document|audio|gif)\s+omitted>/i` (covers `<Media omitted>`, `image omitted`, `sticker omitted`, `document omitted`, `*.pdf • N páginas document omitted`) → `type='omitted'`, `text` = the original marker body preserved verbatim as a visible placeholder (MEDIA-04).
    3. Else if `body.trim()` matches `/^(mensagem apagada|message deleted|this message was deleted)$/i` → `type='deleted'`.
    4. Else if no sender (the `SENDER_RE` fall-through) → `type='system'`, full remainder as `body` (D-11).
    5. Else → `type='text'`.
    - Preserve `author` RAW in every branch (do NOT strip `~ `, spaces, or bidi wrappers — D-09, PARSE-05).
    - Wire `classifyType` into the state machine so the empty-body→attachment merge and same-second burst paths still produce correct types.
    - In `src/model.ts`, collect `warnings` (invalid/out-of-range continuations, override applied) during parse and emit them via the `verboseReport` helper when `opts.verbose` is set (D-07).
  </action>
  <acceptance_criteria>
    - New assertions in `test/classify.test.ts` and the existing `test/tracer.test.ts` (re-run on Notas):
      - Notas line 6 `image omitted` → `type='omitted'`, `text` contains `image omitted`.
      - Notas line 14 `Mensagem apagada` → `type='deleted'`.
      - Notas line 15 `sticker omitted` → `type='omitted'`.
      - Notas line 18 `autorizacao_atividade.pdf • 2 páginas document omitted` → `type='omitted'`, `text` preserves the full marker.
      - Notas line 196 `96980389904-IRPF-D-0-0211-31-07-2026-08-03.pdf • 1 página <attached: ...pdf>` → `type='document'`, `media` = the `.pdf` filename, `text` keeps the leading `96980389904-IRPF-...pdf • 1 página ` caption.
      - Notas line 9 author `Guionardo Furlan` and all `author` values are byte-for-byte the raw sender string (no `~ `/space normalization).
    - `npm test` passes including the updated tracer test (the previously `text`-classified omitted/deleted rows now assert the correct `omitted`/`deleted` types).
    - `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All 8 `type` values are correctly produced: `<attached>`→media type, `* omitted`→omitted, deleted-marker→deleted, no-sender→system, else text; author preserved raw; verbose reporting wired.</done>
  <reversibility rating="costly">D-15's 8-value `type` set is the downstream contract for Phase 2 renderers; changing it later requires touching renderers. User-locked via D-15 — no re-ask.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat content→classification | Untrusted message text is matched against type patterns |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02b | Spoofing/Injection | `omitted`/`deleted` regex matching | low | mitigate | Patterns are anchored/escaped and only classify `type`; no eval, no shell. A message literally reading "Mensagem apagada" is intentionally classified `deleted` (correct per spec). |
| T-01-05 | Information Disclosure | `--verbose` output | low | accept | Verbose prints detected format + filenames already present in the export; no secrets exposed beyond chat content the user already owns. |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages introduced in this plan; reuses fflate/date-fns/commander. |
</threat_model>

<verification>
- `npm test` green (timestamp + classify + updated tracer tests).
- `npx tsx src/index.ts "test/fixtures/notas.zip" --verbose --out /tmp/notas` prints detection report and emits correct omitted/deleted rows.
</verification>

<success_criteria>
The parser detects locale (day/month vote, 12h/24h, separators, 2-digit year, sanity window, invalid→continuation) per-file and classifies every message into one of the 8 `type` values (media, omitted, deleted, system, text) with the raw author preserved. PARSE-03 and the remaining PARSE-07 type granularity are fully satisfied and tested.
</success_criteria>

<output>
Create `.planning/phases/01-parsing-model-core/01-02-SUMMARY.md` when done.

## Artifacts this phase produces (plan 02)
- `src/parse/timestamp.ts` — `detectFormat`, extended `parseTimestamp` (12h/D-M/2-digit-year/sanity-window)
- `src/parse/message.ts` — `classifyType` (omitted/deleted/system fallback)
- `src/model.ts` — two-pass detection + `verboseReport`
- `test/timestamp.test.ts`, `test/classify.test.ts`
</output>
