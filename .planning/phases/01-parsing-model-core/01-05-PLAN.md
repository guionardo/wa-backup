---
phase: 01-parsing-model-core
plan: 05
type: execute
wave: 5
gap_closure: true
gap_ids: [G-01-16]
depends_on: [01-04]
files_modified:
  - src/extract.ts
  - test/integration.test.ts
autonomous: true
requirements: [PARSE-01]
estimate:
  tokens: 14000
  raw_tokens: 7000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "When the export keeps `_chat.txt` at the archive ROOT (real WhatsApp exports), the chat name is derived from the ZIP file basename with `.zip` stripped — `data/WhatsApp Chat - Plataforma WK.zip` → output at `<out>/WhatsApp Chat - Plataforma WK/messages.csv`, never the generic `chat`."
    - "Folder-wrapped exports (synthetic/test zips) keep using the top-level folder name; sanitization (strip `/`, `..`) still applies to both sources."
  artifacts:
    - "src/extract.ts — chatNameFromZip root-level fallback to zip basename"
    - "test/integration.test.ts — root-level _chat.txt naming test"
---

<objective>
Close G-01-16: real WhatsApp exports store `_chat.txt` at the archive root, so the chat-name fallback must derive from the ZIP file name instead of returning the generic `chat`.
</objective>

<context>
@.planning/phases/01-parsing-model-core/01-UAT.md
@src/extract.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive chat name from ZIP basename for root-level exports (G-01-16)</name>
  <files>src/extract.ts, test/integration.test.ts</files>
  <action>
    - In `chatNameFromZip`: when the `_chat.txt` entry has NO top-level folder (`parts.length === 1`), use `path.basename(zipPath).replace(/\.zip$/i, '')` as the raw name (sanitized like today: strip `..` and `/`, trim, fallback `chat` only if empty).
    - Folder-wrapped exports keep current behavior (top-level folder wins).
    - Add integration test: build a zip with `_chat.txt` at ROOT named `WhatsApp Chat - Root Level.zip`; assert CSV lands at `<out>/WhatsApp Chat - Root Level/messages.csv`.
  </action>
  <acceptance_criteria>
    - `npx tsx src/index.ts "data/WhatsApp Chat - Plataforma WK.zip" --out /tmp/uat2 --verbose` writes `/tmp/uat2/WhatsApp Chat - Plataforma WK/messages.csv`.
    - `npm test` all suites green.
  </acceptance_criteria>
  <verify><automated>npm test</automated></verify>
  <done>Output folder is the chat name from the ZIP file name for real exports.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition |
|-----------|----------|-----------|-------------|
| T-01-01 | Tampering | derived folder name | mitigate — basename sanitized (strip `/`, `..`) before mkdir, unchanged from existing guard |
</threat_model>

<verification>
- `npm test` green.
- Manual CLI run against the REAL root-level export writes `<out>/WhatsApp Chat - Plataforma WK/messages.csv`.
</verification>

<success_criteria>
G-01-16 closed: no generic `chat` folder for real exports; name comes from the ZIP file.
</success_criteria>

<output>
Create `.planning/phases/01-parsing-model-core/01-05-SUMMARY.md` when done.
</output>
