---
phase: 05-manifest-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/media-manifest.ts
  - src/media.ts
  - src/model.ts
  - test/media-manifest.test.ts
  - test/media.test.ts
autonomous: true
requirements:
  - MEDIA-07
  - MEDIA-08
estimate:
  tokens: 40000
  raw_tokens: 22000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "After a run, media/manifest.json exists and lists every resolved original ref with { ref, hash(64-hex), relPath, size, mime }; byte-identical refs share the same relPath (MEDIA-07)."
    - "Re-rendering from messages.csv + the output folder (no ZIP — e.g. a later --inline re-render) resolves all media via buildMediaMap reading the manifest, not directory guessing (MEDIA-07)."
    - "Two runs on the same ZIP yield a byte-identical media/ folder; the canonical file for a duplicate set is always the first-occurrence ref by stable message order (idempotent, MEDIA-08)."
    - "A pre-v1.1 backup (media named by original filename, no manifest) still renders via the legacy directory-scan fallback inside buildMediaMap, keeping existing test/media.test.ts assertions green (MEDIA-07 fallback)."
  artifacts:
    - "src/media-manifest.ts (new module: MediaManifestEntry, MediaManifest, writeManifest, readManifest, legacyScan)."
    - "media/manifest.json written atomically by reconcileMedia at end of extraction."
    - "ReconcileResult.duplicatesRemoved + ReconcileResult.bytesSaved (additive)."
    - "buildMediaMap manifest-first branch + legacyScan fallback."
    - "MediaEntry.hash optional field (carried from manifest; renderers ignore it)."
    - "runParser stderr report surfaces duplicatesRemoved / bytesSaved whenever > 0."
  key_links:
    - "reconcileMedia writes manifest.json <-> buildMediaMap reads it first (the bridge that closes the re-render break)."
    - "Atomic write (media/.tmp-<uuid>.json -> renameSync manifest.json) so a crashed run never leaves a half-written manifest that a later buildMediaMap would trust."
---

<objective>
Persist a `media/manifest.json` that maps every original media ref to its content-addressed
canonical file, and switch `buildMediaMap` to read it first (manifest-first) with a legacy
directory-scan fallback for pre-v1.1 folders. Delivers MEDIA-07 (manifest artifact + manifest-first
buildMediaMap with fallback) and MEDIA-08 (deterministic first-occurrence canonical selection).

Purpose: re-rendering from `messages.csv` (no ZIP) and reproducible re-runs become reliable —
the ref→file link is persisted instead of guessed by normalized filename.
Output: new `src/media-manifest.ts`, an atomic `media/manifest.json`, extended `ReconcileResult`,
a manifest-first `buildMediaMap`, and an enhanced stderr dedup report. Renderers / `Message` /
`messages.csv` are intentionally UNCHANGED.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-manifest-bridge/05-CONTEXT.md
@.planning/research/ARCHITECTURE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@src/media.ts
@src/model.ts
@src/parse/types.ts
@test/media.test.ts
</context>

<tasks>

<task type="tracer">
  <name>End-to-end manifest bridge — write manifest in reconcileMedia, read it in buildMediaMap</name>
  <files>src/media-manifest.ts, src/media.ts, test/media-manifest.test.ts</files>
  <read_first>
    - src/media.ts (reconcileMedia, buildMediaMap, ReconcileResult, MediaEntry, canonicalMediaName, mimeFromExt, isInlineable, normalizeMediaName)
    - src/parse/types.ts (Message shape)
    - .planning/phases/05-manifest-bridge/05-CONTEXT.md (D-05.1, D-05.2, schema, write policy)
    - .planning/research/ARCHITECTURE.md (Steps 3 & 6: media-manifest module, buildMediaMap manifest-first + legacy fallback)
  </read_first>
  <action>
    Create new module src/media-manifest.ts (pure, no ZIP import) with these exported symbols:

    - `interface MediaManifestEntry { ref: string; hash: string; relPath: string; size: number; mime: string; }`
    - `interface MediaManifest { version: number; generatedAt: string; entries: MediaManifestEntry[]; unresolved: string[]; duplicatesRemoved: number; bytesSaved: number; }`
    - `writeManifest(mediaDir: string, manifest: MediaManifest): void` — write to
      `path.join(mediaDir, '.tmp-' + randomUUID() + '.json')` then `fs.renameSync(tmp,
      path.join(mediaDir, 'manifest.json'))` (atomic, D-05 write policy).
    - `readManifest(manifestPath: string): MediaManifest` — `JSON.parse(fs.readFileSync(manifestPath,'utf8'))`;
      throw if `entries` is not an array (defensive parse).
    - `legacyScan(mediaDir: string): Map<string, MediaEntry>` — scan `mediaDir`, skip directories,
      skip `manifest.json`, skip names starting with `._` or `.tmp-`; build `normalizeMediaName(actual)
      -> MediaEntry { relPath: 'media/'+actual, mime, size, inlineable }`. Keyed by normalized name.

    Modify src/media.ts:
    - Import `writeManifest`, `readManifest`, `legacyScan` from './media-manifest'.
    - Extend `ReconcileResult` with `duplicatesRemoved: number;` and `bytesSaved: number;`.
    - Add optional `hash?: string;` to `MediaEntry`.
    - In `reconcileMedia`: after the existing `refsByNorm` / `index` setup, track
      `const committedHashes = new Set<string>(); let duplicatesRemoved = 0; let bytesSaved = 0;`
      and a `const manifestEntries: MediaManifestEntry[] = []`. For each `[norm, refList]` that
      matches a `meta`: extract via `extractEntry` to a `.tmp-<uuid>` as today; compute
      `canonicalName = canonicalMediaName(hash, ext)` (16-hex prefix, D-01) and
      `canonicalPath = path.join(mediaDir, canonicalName)`. If `committedHashes.has(hash)`:
      `fs.unlinkSync(tmp)` (dedup) and count ALL refs in `refList` as duplicates
      (`duplicatesRemoved += refList.length; bytesSaved += refList.length * size`). Otherwise:
      `fs.existsSync(canonicalPath)` ? `unlinkSync(tmp)` (D-04 trust-stream, no rewrite) :
      `renameSync(tmp, canonicalPath)`; `committedHashes.add(hash)`; count the within-norm
      redundancy (`duplicatesRemoved += Math.max(0, refList.length - 1); bytesSaved +=
      Math.max(0, refList.length - 1) * size`). Build the `MediaEntry` INCLUDING `hash` and set it
      for every ref in `refList` (all share the same entry). Push one `MediaManifestEntry`
      `{ ref, hash, relPath, size, mime }` per ref (D-05.2 — one entry per original ref). Keep
      `resolved`/`resolvedSet`/`mediaMap` semantics identical to today.
    - After `Promise.all(writes)`, compute `unresolved` exactly as today and call
      `writeManifest(mediaDir, { version: 1, generatedAt: new Date().toISOString(), entries:
      manifestEntries, unresolved, duplicatesRemoved, bytesSaved })`. Return the extended
      `ReconcileResult`.
    - In `buildMediaMap`: BEFORE the existing `activeReconcileMap` short-circuit, check
      `path.join(mediaDir,'manifest.json')`. If it exists: `readManifest` it; build
      `byRef = new Map(manifest.entries.map(e => [e.ref, e]))`; for each message ref look it up;
      if found AND `fs.existsSync(path.join(mediaDir, e.relPath))` is true, add
      `{ relPath: e.relPath, mime: e.mime, size: e.size, inlineable: isInlineable(e.mime, e.size),
      hash: e.hash }` to the map (EXCLUSIVE/authoritative: file missing on disk => absent/placeholder,
      never re-scanned). Keep the `activeReconcileMap` short-circuit and the legacy directory-scan
      branch, but replace the legacy branch body with a call to `legacyScan(mediaDir)` and resolve
      each message ref via `legacyScan(...).get(normalizeMediaName(m.media))`.

    Create test/media-manifest.test.ts (tracer verification — proves the full write+read path):
    reconcileMedia on the Notas fixture, then assert `media/manifest.json` exists, parses,
    `version === 1`, `entries.length === 17`, every entry has `hash.length === 64`,
    `relPath` matches `/^media\/[0-9a-f]{16}\.[a-z0-9]+$/`, `size > 0`, `mime` non-empty; then
    buildMediaMap(outDir, messages) resolves the video ref and its relPath equals the manifest
    entry's relPath. This is the single happy path end-to-end.
  </action>
  <verify>
    <automated>node --import tsx --test "test/media-manifest.test.ts"</automated>
  </verify>
  <done>
    A run writes media/manifest.json with one entry per original ref (full 64-hex hash, CAS
    relPath). buildMediaMap resolves refs by reading the manifest (manifest-first) and the tracer
    test passes. No renderer / Message / csv change was made.
  </done>
</task>

<task type="auto">
  <name>Surface dedup savings on stderr in runParser (no signature change)</name>
  <files>src/model.ts</files>
  <read_first>
    - src/model.ts (runParser media report block, lines 141-154; ReconcileResult usage)
    - src/media.ts (ReconcileResult.duplicatesRemoved, bytesSaved added in Task 1)
    - .planning/phases/05-manifest-bridge/05-CONTEXT.md (runParser report fields)
  </read_first>
  <action>
    In runParser, extend the existing `[wa-backup] media:` stderr block (currently gated on
    `opts.verbose || resolved.length + unresolved.length > 0`) so it ALSO prints when
    `mediaReport.duplicatesRemoved > 0`. Append a cyan segment when savings exist:
    ` ${mediaReport.duplicatesRemoved} duplicate(s) removed (${formatBytes(mediaReport.bytesSaved)} saved)`.
    Add a local `formatBytes(n: number): string` helper in model.ts that renders `n` as e.g.
    `"4.2 MB"` (>=1e6 -> MB with one decimal; >=1e3 -> KB; else bytes). Do NOT change the
    runParser signature, the reconcileMedia->renderOutputs ordering, or any other output. Keep the
    existing per-ref `unresolved:` yellow lines.
  </action>
  <verify>
    <automated>node --import tsx --test "test/media.test.ts" 2>&1 | grep -E "duplicate|saved" || echo "no-dup-run-ok"</automated>
  </verify>
  <done>
    When a run finds duplicates, stderr contains "duplicate(s) removed" and a "(X MB/X KB saved)"
    figure. When there are no duplicates the existing resolved/unresolved line is unchanged. The
    runParser signature and return type are untouched.
  </done>
</task>

<task type="auto">
  <name>Keep test/media.test.ts green + add the four required manifest coverages</name>
  <files>test/media.test.ts, src/media.ts</files>
  <read_first>
    - test/media.test.ts (reconcileMedia Notas test lines 55-98 file-count loop; buildMediaMap
      '(1)' fallback test lines 126-154; CAS test lines 100-124)
    - src/media.ts (writeManifest atomic, legacyScan, manifest-first buildMediaMap)
    - .planning/research/ARCHITECTURE.md (Test Impact: legacy scan fallback keeps line 143 green;
      reconcileMedia file count may change)
  </read_first>
  <action>
    In test/media.test.ts reconcileMedia Notas test (lines 55-98): the media/ dir now also contains
    `manifest.json`. Update the file enumeration to ignore it and any `.tmp-*` so the CAS assertions
    stay about media files only: `const files = fs.readdirSync(path.join(outDir,'media'))
    .filter(f => f !== 'manifest.json' && !f.startsWith('.tmp-'))`. Keep
    `files.length >= 1 && files.length <= 17` and the `/^[0-9a-f]{16}\.[a-z0-9]+$/i` per-file check
    (manifest.json is already filtered out). Do NOT otherwise alter the 17-resolved / 0-unresolved
    assertions.

    Add coverage (same file or new test blocks) for the four required cases:
    1. round-trip: writeManifest then readManifest returns an equal-shaped object (already exercised
       by the tracer test; add a focused assertion here too if desired).
    2. two byte-identical refs => one physical file + two manifest entries: zipSync two identical
       PNGs `A.png`/`B.png`, reconcileMedia(dir, ['A.png','B.png']); assert
       `fs.readdirSync(media).filter(f=>!f.startsWith('.tmp-') && f!=='manifest.json').length === 1`,
       `res.mediaMap.size === 2`, `manifest.entries.length === 2`, both entries share the same
       `relPath` and `hash`, `res.duplicatesRemoved === 1`, `res.bytesSaved > 0`.
    3. legacy fallback: a temp media/ folder containing only `photo (1).jpg` (NO manifest.json,
       setActiveReconcileMap(null)), buildMediaMap resolves ref `photo.jpg` to
       `media/photo (1).jpg` — proving the fallback path, not the manifest path, is used.
    4. bytesSaved > 0 on a known-duplicate export: covered by case 2's `bytesSaved > 0` assertion.

    Confirm the existing line-143 `entry!.relPath === 'media/photo (1).jpg'` assertion still passes
    (it builds a folder without a manifest, so legacyScan is used).
  </action>
  <verify>
    <automated>node --import tsx --test "test/media.test.ts" "test/media-manifest.test.ts"</automated>
  </verify>
  <done>
    `npm test` (both test files) is green. The existing legacy '(1)' assertion (line 143) passes via
    the fallback. New assertions prove: manifest round-trip, one-file-two-entries dedup with
    bytesSaved>0, and legacy fallback resolution. Renderers / Message / csv remain untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → local filesystem | Tool reads a local export ZIP and writes local output (media/, manifest.json). No network trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-01 | Tampering | readManifest parse of media/manifest.json | low | mitigate | Validate shape in readManifest (throw if `entries` is not an array); never execute manifest content; manifest is generated by this tool from the ZIP, not from untrusted remote input. |
| T-05-02 | Tampering | package installs | low | accept | No new runtime or dev dependencies introduced (D-06). Package-legitimacy gate not applicable. |
| T-05-03 | Information Disclosure | media/ + manifest.json are local-only outputs | low | accept | No secrets; output opens standalone in a browser. No PII beyond what the user's own export contains. |
</threat_model>

<verification>
- `node --import tsx --test "test/media-manifest.test.ts"` passes (manifest write + manifest-first read, full 64-hex hash, CAS relPath).
- `node --import tsx --test "test/media.test.ts"` passes (legacy fallback green at line 143; file-count loop filters manifest.json).
- Manual sanity: on the Notas fixture, `media/manifest.json` contains 17 entries each with 64-hex hash; a second identical run yields a byte-identical `media/` (idempotent, MEDIA-08).
- Renderers, `Message`, and `messages.csv` schema are confirmed unchanged (no task edits them).
</verification>

<success_criteria>
- [ ] `media/manifest.json` is written atomically by reconcileMedia listing every resolved ref with `{ ref, hash(64-hex), relPath, size, mime }` (MEDIA-07).
- [ ] buildMediaMap reads the manifest first; legacy directory-scan fallback (excludes manifest.json/.tmp-*) resolves pre-v1.1 folders (MEDIA-07).
- [ ] Duplicate sets always pick the first-occurrence canonical copy; two runs on the same ZIP are byte-identical (MEDIA-08).
- [ ] ReconcileResult carries duplicatesRemoved + bytesSaved; runParser prints them to stderr when > 0.
- [ ] test/media.test.ts stays green via the legacy fallback; new coverage asserts round-trip, one-file-two-entries, and bytesSaved > 0.
- [ ] Renderers / Message / messages.csv unchanged.
</success_criteria>

<artifacts_this_phase_produces>
New file `src/media-manifest.ts`:
- `interface MediaManifestEntry { ref: string; hash: string; relPath: string; size: number; mime: string; }`
- `interface MediaManifest { version: number; generatedAt: string; entries: MediaManifestEntry[]; unresolved: string[]; duplicatesRemoved: number; bytesSaved: number; }`
- `writeManifest(mediaDir, manifest)` — atomic (`.tmp-<uuid>.json` -> renameSync `manifest.json`)
- `readManifest(manifestPath)` — parse + shape guard
- `legacyScan(mediaDir)` — `Map<normalizedName, MediaEntry>` excluding manifest.json / `.tmp-*` / `._`

Modified `src/media.ts`:
- `reconcileMedia` — collects every original ref, computes `duplicatesRemoved` / `bytesSaved`, writes `media/manifest.json` at end of extraction; dedup decision: `committedHashes` set + `existsSync` check.
- `buildMediaMap` — manifest-first branch (read manifest, exclusive: file-missing => absent) then `activeReconcileMap` short-circuit then `legacyScan` fallback.
- `ReconcileResult` — adds `duplicatesRemoved: number`, `bytesSaved: number`.
- `MediaEntry` — adds optional `hash?: string`.

Modified `src/model.ts`:
- `runParser` media report — appends `N duplicate(s) removed (X saved)` to stderr whenever `duplicatesRemoved > 0`; local `formatBytes` helper. No signature change.

New tests:
- `test/media-manifest.test.ts` (tracer end-to-end: write+read manifest, 17 entries, CAS relPath).
- Extended `test/media.test.ts` — file-count loop filters `manifest.json`/`.tmp-*`; new assertions: one-file-two-entries + `bytesSaved > 0`, legacy fallback.

Explicitly UNCHANGED: `src/render/{json,md,html}.ts`, `src/parse/types.ts` (`Message`), `src/csv.ts` (`messages.csv`), `src/index.ts`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/05-manifest-bridge/05-01-SUMMARY.md` when done executing.
</output>
