---
phase: 01-parsing-model-core
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/csv.ts
  - src/model.ts
  - test/integration.test.ts
autonomous: true
requirements:
  - PARSE-07
estimate:
  tokens: 52000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "On a SECOND run against an existing CSV, new messages are MERGED in: a row is a duplicate only if `(timestamp_iso, author, text, media)` ALL match; duplicates are skipped (D-16)."
    - "After every merge/append the CSV is kept ordered by `timestamp_iso` ASCENDING (lexicographic ISO sort == chronological); equal timestamps preserve insertion order (stable, D-17)."
    - "The dedupe key uses the ASCII Unit-Separator (0x1F) to join fields so legitimate chat content (which never contains 0x1F) cannot collide (D-16)."
    - "Incremental append is the mechanism Phase 3/4 will use to add media-reconciled rows without re-parsing the whole chat (D-13 source-of-truth contract)."
    - "Final verification parses BOTH real samples end-to-end and asserts correct sender/timestamp/type/media for known lines, dedupe behavior, and ascending ordering — no phantom empty rows."
  artifacts:
    - "src/csv.ts — `mergeCsv(path, newMessages)`, `dedupeKey(m)`, `readCsv(path)`"
    - "src/model.ts — `runParser` calls `mergeCsv` (load existing → dedupe → stable sort → rewrite) instead of blind `writeCsv` overwrite"
    - "test/integration.test.ts — full both-sample assertions + dedupe + ordering"
  key_links:
    - "mergeCsv must preserve the header and UTF-8-no-BOM encoding (D-19)"
    - "stable sort must not reorder equal-timestamp rows (same-second burst integrity from plan 01)"
---

<objective>
Completes the CSV source-of-truth contract with incremental append/merge (dedupe + stable ascending sort) and locks in end-to-end correctness with a comprehensive integration test over both real exports.

Purpose: Satisfy D-16/D-17 (dedupe + ordering) and provide the phase's definitive verification that PARSE-07's normalized model is correct on real data.
Output: `mergeCsv` + `readCsv` in csv.ts; full integration test.
</objective>

<execution_context>
@/Users/guionardo/.config/opencode/gsd-core/workflows/execute-plan.md
@/Users/guionardo/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-parsing-model-core/01-CONTEXT.md
@.planning/phases/01-parsing-model-core/01-RESEARCH.md
@data/WhatsApp Chat - Plataforma WK/_chat.txt
@data/WhatsApp Chat - Notas pessoais/_chat.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSV merge / dedupe / stable sort (D-16, D-17)</name>
  <files>src/csv.ts, src/model.ts</files>
  <read_first>
    - src/csv.ts (current `writeCsv`/`csvRow` from plan 01)
    - .planning/phases/01-parsing-model-core/01-RESEARCH.md (§4.5 incremental append/merge, dedupe key with 0x1F)
    - .planning/phases/01-parsing-model-core/01-CONTEXT.md (D-16 dedupe key, D-17 ascending order)
  </read_first>
  <action>
    Extend `src/csv.ts`:
    - `dedupeKey(m: Message): string` = `[m.timestamp_iso, m.author, m.text, m.media].join('\u001f')` (Unit-Separator 0x1F — cannot appear in chat content, D-16).
    - `readCsv(path): Message[]` — parse the RFC-4180 file back into `Message[]` (reverse `csvField` quoting; skip the header row).
    - `mergeCsv(path, newMessages: Message[]): number` — if file exists: load existing, build a `Set` of existing `dedupeKey`s; append only `newMessages` whose key is NOT already present (D-16). Combine existing + new, then STABLE-sort all rows by `timestamp_iso` ascending (preserve insertion order for equal timestamps — use `Array.prototype.sort` with an index tiebreaker, D-17). Rewrite the file with header + rows via the existing utf8 writer (NO BOM, D-19). Return the number of NEW rows added.
    - Keep `writeCsv` for the first-write / overwrite case (used by tests that start fresh).
    - In `src/model.ts` `runParser`: after collecting `messages`, call `mergeCsv(path.join(dir,'messages.csv'), messages)` instead of `writeCsv` so re-runs merge incrementally. Return the added-count from `mergeCsv`.
  </action>
  <acceptance_criteria>
    - New `test/csv.test.ts` (node:test):
      - Writing then `mergeCsv`-ing the SAME messages yields the SAME row count (no duplicates, D-16).
      - `mergeCsv` with 3 new distinct messages + 1 duplicate of an existing row → final count = original + 3 (duplicate skipped).
      - After merge the rows are sorted by `timestamp_iso` ascending; two rows sharing `2026-07-24T10:41:10` keep their original relative order (stable, same-second burst preserved).
      - The rewritten file still starts with `timestamp_iso,type,author,text,media` and has NO BOM (first 3 bytes != EF BB BF).
    - `npm test` passes; `npm run build` succeeds.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Incremental append merges without duplicates, keeps the file sorted ascending and BOM-free; the source-of-truth contract (D-13/D-16/D-17/D-19) is complete.</done>
  <reversibility rating="reversible">D-16/D-17 merge semantics can be adjusted later without breaking a persisted external contract; CSV remains the single source of truth.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Full end-to-end verification on both real samples</name>
  <files>test/integration.test.ts, src/index.ts</files>
  <read_first>
    - src/model.ts, src/csv.ts, src/parse/message.ts (all implemented across plans 01-02)
    - data/WhatsApp Chat - Plataforma WK/_chat.txt
    - data/WhatsApp Chat - Notas pessoais/_chat.txt
    - .planning/phases/01-parsing-model-core/01-CONTEXT.md (specifics: cover both fixtures with at least one real-export parse assertion)
  </read_first>
  <action>
    Create `test/integration.test.ts` that is the phase's authoritative acceptance test:
    - Builds ZIPs from BOTH real `data/.../_chat.txt` (fflate `zipSync`, `<chat>/_chat.txt` layout) into `test/fixtures/` (gitignored).
    - Runs `runParser(zip, { out: tmpDir })` for each and reads back `messages.csv`.
    - **Plataforma WK** assertions (superset of tracer): header + no BOM; first row `2026-07-23T09:47:18,text,Plataforma WK,Messages and calls are end-to-end encrypted...`; line 10 sticker row; lines 20-21 continuation joined with `Senha: TIMEWK2026`; lines 36-37 merged single `photo` row (NO phantom empty); lines 46-51 FIVE distinct `photo` rows; line 57 `sticker omitted` → `type='omitted'`; line 100 `Mensagem apagada` → `type='deleted'`; line 9 raw bidi author; every `timestamp_iso` matches ISO regex.
    - **Notas pessoais** assertions: header + no BOM; line 1 text; line 18 `document omitted` → `type='omitted'` with full marker text; line 37 sticker attach; line 124 caption+attachment `photo` with caption in `text`; line 196 document attach with `IRPF` caption; lines 14/15 `deleted`/`omitted` respectively; author always raw.
    - **Dedupe/merge**: run `runParser` TWICE on the same ZIP into the SAME out dir; assert the second run adds 0 new rows and total row count is unchanged (D-16 in action).
    - **Ordering**: assert every `messages.csv` is sorted by `timestamp_iso` ascending (D-17).
    - **No phantom**: assert no row has empty `text` AND empty `media` that is immediately followed by a media row for the same author (catches empty-body merge regressions).
    - Confirm `src/index.ts` passes `opts` through so a real CLI invocation (`npx tsx src/index.ts <zip> --out <dir>`) exercises the same path.
  </action>
  <acceptance_criteria>
    - `npm test` is GREEN across ALL suites (tracer + timestamp + classify + csv + integration).
    - The integration test parses BOTH real exports and asserts correct sender / timestamp_iso / type / media for known lines, dedupe-on-rerun, and ascending ordering — directly satisfying "Cover them with at least one test that parses a real export and asserts correct sender/timestamp/type/media extraction."
    - No TypeScript `strict` errors; `npm run build` succeeds and `dist/index.js` runs `node dist/index.js <zip> --out <dir>`.
    - `grep -c "mergeCsv" src/model.ts` confirms the CLI path uses merge (not blind overwrite).
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>Phase 1 is fully verified: both real exports parse into a correct, deduplicated, sorted, BOM-free CSV with all 8 `type` values; every PARSE-01..07 requirement is covered and tested.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→CSV | Existing CSV is read back and rewritten during merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering | `readCsv` re-parse | low | mitigate | `readCsv` trusts only the RFC-4180 quoting we wrote; malformed external edits are re-emitted as-is (no injection path). Merge only adds rows; never deletes user data. |
| T-01-SC | Tampering | npm installs | high | mitigate | No new packages; reuses existing stack. |
</threat_model>

<verification>
- `npm test` green (all 5 suites).
- `npm run build` succeeds; `node dist/index.js test/fixtures/plataforma.zip --out /tmp/wk` produces the final CSV.
</verification>

<success_criteria>
The CSV source-of-truth supports incremental append (dedupe by the 4-field key, no duplicates) and stays sorted ascending and BOM-free. Both real WhatsApp exports parse end-to-end into a normalized model with correct sender, ISO timestamp, 8-value type, and media reference, verified by an authoritative integration test. All Phase 1 requirements (PARSE-01..07) are complete and tested.
</success_criteria>

<output>
Create `.planning/phases/01-parsing-model-core/01-03-SUMMARY.md` when done.

## Artifacts this phase produces (plan 03)
- `src/csv.ts` — `dedupeKey`, `readCsv`, `mergeCsv`
- `src/model.ts` — `runParser` now calls `mergeCsv`
- `test/integration.test.ts`, `test/csv.test.ts`

## PLANNING COMPLETE
All three plans (01-01 tracer, 01-02 hardening, 01-03 merge/verify) are written. Phase 1 covers PARSE-01..07, honors every locked decision D-01..D-19, and produces the CSV source-of-truth `messages.csv`.
</output>
