---
phase: 01-parsing-model-core
plan: 07
type: execute
wave: 7
gap_closure: true
gap_ids: [G-01-19]
depends_on: [01-06]
files_modified:
  - src/index.ts
autonomous: true
requirements: [PARSE-01]
estimate:
  tokens: 8000
  raw_tokens: 4000
  tasks: 1
  confidence: high

must_haves:
  truths:
    - "The CLI success line prints the REAL resolved path, e.g. `✓ Merged 134 new message(s) into output/plataforma-wk/messages.csv` — never the literal `<chat>` placeholder."
  artifacts:
    - "src/index.ts — interpolate chatNameFromZip result into the success message"
---

<objective>
Close G-01-19: the CLI success message must show the actual output path instead of the literal `<chat>` placeholder.
</objective>

<context>
@.planning/phases/01-parsing-model-core/01-UAT.md
@src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Interpolate real chat folder into success message (G-01-19)</name>
  <files>src/index.ts</files>
  <action>
    - In the action handler, after `runParser` resolves, call `chatNameFromZip(zip)` and print `path.join(outDir, chat, 'messages.csv')` where `outDir = opts.out ?? 'out'`.
    - Keep the green `✓ Merged N new message(s)` prefix.
  </action>
  <acceptance_criteria>
    - `npx tsx src/index.ts "data/WhatsApp Chat - Plataforma WK.zip" --out /tmp/uat4` prints `... /tmp/uat4/plataforma-wk/messages.csv`.
    - `npm test` still green; build succeeds.
  </acceptance_criteria>
  <verify><automated>npm test && npm run build</automated></verify>
  <done>Success message shows the real output path.</done>
</task>

</tasks>

<threat_model>
None new — display-only change.
</threat_model>

<verification>
- Manual CLI run shows resolved path.
- `npm test` green.
</verification>

<success_criteria>
G-01-19 closed.
</success_criteria>

<output>
Create `.planning/phases/01-parsing-model-core/01-07-SUMMARY.md` when done.
</output>
